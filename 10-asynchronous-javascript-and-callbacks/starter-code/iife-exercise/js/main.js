// Write an IIFE function that takes a duration argument.
// The function will automatically execute and display a count at every second
// until the specified duration.
// Use the setTimeout function to count up.
// Hint: a second is the timer passed * 1000 (milliseconds).
